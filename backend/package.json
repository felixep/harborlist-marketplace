{
  "name": "boat-listings-backend",
  "version": "1.0.0",
  "description": "Backend Lambda functions for boat listing platform",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && npm run package",
    "package": "cd dist && mkdir -p packages && zip -r packages/auth-service.zip auth-service/ shared/ types/ && zip -r packages/listing.zip listing/ shared/ types/ && zip -r packages/search.zip search/ shared/ types/ && zip -r packages/media.zip media/ shared/ types/ && zip -r packages/email.zip email/ shared/ types/ && zip -r packages/stats-service.zip stats-service/ shared/ types/ && zip -r packages/admin-service.zip admin-service/ shared/ types/",
    "watch": "tsc -w",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist",
    "create-admin": "ts-node scripts/create-admin-user.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/client-opensearchserverless": "^3.400.0",
    "@aws-sdk/client-s3": "^3.400.0",
    "@aws-sdk/client-ses": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0",
    "@aws-sdk/s3-request-presigner": "^3.400.0",
    "aws-lambda": "^1.0.7",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "sharp": "^0.32.6",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/bcryptjs": "^2.4.2",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/node": "^20.5.0",
    "@types/uuid": "^9.0.2",
    "@typescript-eslint/eslint-plugin": "^6.4.0",
    "@typescript-eslint/parser": "^6.4.0",
    "eslint": "^8.47.0",
    "jest": "^29.6.2",
    "ts-jest": "^29.4.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
